{
  "name": "assertLanguage",
  "description": "Returns the user-visible strings on screen that are not written in the expected language",
  "strict": true,
  "schema": {
    "type": "object",
    "required": ["violations"],
    "additionalProperties": false,
    "properties": {
      "violations": {
        "type": "array",
        "items": {
          "type": "object",
          "required": ["text", "detectedLanguage", "reasoning"],
          "additionalProperties": false,
          "properties": {
            "text": {
              "type": "string",
              "description": "The offending string, copied exactly as it appears on screen"
            },
            "detectedLanguage": {
              "type": "string",
              "description": "The language the string appears to be written in"
            },
            "reasoning": {
              "type": "string",
              "description": "Why this string is a translation gap rather than an allowed exception"
            }
          }
        }
      }
    }
  }
}
